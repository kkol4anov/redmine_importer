<% content_for :header_tags do %>
  <%= stylesheet_link_tag 'importer', :plugin => 'redmine_importer' %>
<% end %>

<h2><%= l(:label_import_result) %></h2>

<p><%= l(:label_result_notice,
  :handle_count => @handle_count,
  :success_count => @handle_count) %>
</p>

<ul class="importer-result-summary">
  <li><%= l(:label_result_created, :count => Array(@created_issues).size) %></li>
  <li><%= l(:label_result_updated, :count => Array(@updated_issues).size) %></li>
  <li><%= l(:label_result_skipped, :count => @skip_count.to_i) %></li>
  <li><%= l(:label_result_errors, :count => @failed_count.to_i) %></li>
</ul>

<%= render :partial => 'imported_issues',
           :locals => { :title => l(:label_result_created_issues,
                                    :count => Array(@created_issues).size),
                        :issues => Array(@created_issues) } %>

<%= render :partial => 'imported_issues',
           :locals => { :title => l(:label_result_updated_issues,
                                    :count => Array(@updated_issues).size),
                        :issues => Array(@updated_issues) } %>

<p><%= l(:label_result_projects) %>
  <br/>
  <% @affect_projects_issues.each do |project, count|%>
    <label class="tabular2"><%= project %>:</label>
    <label class="tabular2"><%= l(:label_result_issues, count) %></label>
    <br/>
  <% end %>
</p>

<% if not @messages.empty? %>
  <hr/>
  <h3><%= l(:label_result_messages) %></h3>
  <ul>
    <% @messages.each do |m| %>
      <li><%= m %></li>
    <% end %>
  </ul>
<% end %>

<hr/>
<% if @failed_count > 0 %>
  <h3><%= l(:label_result_failed, @failed_count) %></h3>
  <table class="list">
    <thead>
      <tr>
        <th>#</th>
        <% @headers.each do |column| %>
          <th><%= column.unpack('U*').pack('U*')  %></th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <% @failed_issues.each do |id, issue| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td><%= id %></td>
          <% issue.each do |column| %>
            <%- data = column[1] -%>
            <%- data = data.unpack('U*').pack('U*') if data.is_a?(String) -%>
            <%= content_tag 'td', data %>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
